# Base de données (dev: SQLite ; prod: postgresql://...)
DATABASE_URL="file:./dev.db"
# Secret de signature des sessions (générer une valeur aléatoire forte en prod)
AUTH_SECRET="change-me"
# Clé API Claude — si absente, la génération IA bascule en mode démo déterministe
ANTHROPIC_API_KEY=""
